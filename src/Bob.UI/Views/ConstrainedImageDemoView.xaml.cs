using Bob.UI.ViewModels;
using Okra.Navigation;

namespace Bob.UI.Views
{
    [PageExport(typeof(ConstrainedImageDemoViewModel))]
    public sealed partial class ConstrainedImageDemoView
    {
        public ConstrainedImageDemoView()
        {
            InitializeComponent();
        }
    }
}
